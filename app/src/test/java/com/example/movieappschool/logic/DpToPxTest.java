package com.example.movieappschool.logic;

import org.junit.Test;
import android.content.Context;
import androidx.test.core.app.ApplicationProvider;
import static org.junit.jupiter.api.Assertions.*;
import static org.junit.Assert.assertThat;

public class DpToPxTest {

    private Context context = ApplicationProvider.getApplicationContext();

    @Test
    public void testDpToPxRequiresDpHasPositiveEnsuresFloat() {
/*        Converter cv = new Converter();
        float dp = 1;
        float result = cv.dpToPx(context, dp);
        assertThat(result)*/

//        Converter cv = new Converter();
//        float dp = 1;
//        float result = cv.dpToPx(dp);
//        assertEquals(float, result);
    }
}
